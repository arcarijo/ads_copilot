# Dependencies
node_modules/

# Next.js
.next/
next-env.d.ts

# Environment / secrets — ignore ALL env files, keep only the examples
.env
.env.*
!.env.example
!.env.redteam.example

# Red-team cycle output (reports + scanner artifacts — never commit)
scripts/redteam/report/report-*.md
scripts/redteam/report/report-*.json
scripts/redteam/report/*.html
.redteam-gitleaks.json

# Databases
*.db
*.db-journal
*.sqlite
prisma/dev.db

# Claude / local tooling
.claude_memory/
.claude/settings.local.json

# Vercel
.vercel

# Editor / OS / misc
*.bak
.DS_Store
Thumbs.db

# TypeScript incremental build info
*.tsbuildinfo

# Installed agent skills (tooling, not app code)
.agents/
